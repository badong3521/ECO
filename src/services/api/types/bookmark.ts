export type BookmarkOverviewType = {
  eventBookmarked: boolean;
  merchantBookmarked: boolean;
  newsBookmarked: boolean;
  promotionBookmarked: boolean;
  lixiBookmarked: boolean;
  totalBookmarked: number;
};
