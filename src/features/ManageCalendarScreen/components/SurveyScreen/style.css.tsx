import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  loading: {
    position: 'absolute',
    left: 0,
    right: 0,
    bottom: 0,
    top: 0,
    elevation: 10,
    zIndex: 10,
  },
  container: {
    height: '100%',
    width: '100%',
  },
});
